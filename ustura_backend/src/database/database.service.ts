import { Injectable } from '@nestjs/common';

@Injectable()
export class DatabaseService {
  // TODO: pg-pool bağlantı yönetimi eklenecek
  // - Pool oluşturma (onModuleInit)
  // - query() metodu
  // - Pool kapatma (onModuleDestroy)
}
