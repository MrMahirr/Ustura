import { Controller } from '@nestjs/common';

@Controller('reservations')
export class ReservationController {
  // TODO: POST   /reservations              (customer)
  // TODO: GET    /reservations/my            (customer)
  // TODO: GET    /reservations/salon/:id     (owner/receptionist)
  // TODO: DELETE /reservations/:id           (customer|owner)
}
