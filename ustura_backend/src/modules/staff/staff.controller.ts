import { Controller } from '@nestjs/common';

@Controller('salons/:salonId/staff')
export class StaffController {
  // TODO: GET    /salons/:salonId/staff
  // TODO: POST   /salons/:salonId/staff           (owner)
  // TODO: PATCH  /salons/:salonId/staff/:staffId   (owner)
  // TODO: DELETE /salons/:salonId/staff/:staffId   (owner)
}
