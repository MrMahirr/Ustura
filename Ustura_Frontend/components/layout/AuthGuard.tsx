import { ReactNode } from 'react';

// TODO: JWT kontrol — token yoksa /giris'e yönlendir
// TODO: Rol kontrolü — yetkisi yoksa /panel'e yönlendir
export default function AuthGuard({ children }: { children: ReactNode }) {
  return null;
}
